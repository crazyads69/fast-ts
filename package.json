{
  "name": "fast-ts",
  "version": "0.0.1-alpha",
  "description": "TypeScript → Go compiler for WinterTC server APIs",
  "main": "dist/cli/index.js",
  "types": "dist/compiler/types.d.ts",
  "bin": {
    "fast-ts": "dist/cli/index.js"
  },
  "files": [
    "dist/",
    "src/runtime/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run tests/integration.test.ts",
    "fast-ts": "npx tsx src/cli/index.ts",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run lint && npm run build && npm test"
  },
  "keywords": [
    "typescript",
    "go",
    "golang",
    "compiler",
    "transpiler",
    "wintertc",
    "wintercg",
    "ecma-429",
    "aot",
    "native",
    "binary"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.19.11",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "commander": "^13.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
